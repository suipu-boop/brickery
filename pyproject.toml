[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "brickery"
version = "0.1.0"
description = "Brickery —— agent 底座内核：brick_runtime / skill_contract / memory / runtime 引擎"
requires-python = ">=3.9"
license = { text = "MIT" }
dependencies = []

[tool.setuptools.packages.find]
include = ["brickery*"]
